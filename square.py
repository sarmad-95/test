"""Utility functions for numeric operations."""


def square(number: float) -> float:
    """Return the square of a number.

    Args:
        number: The value to square.

    Returns:
        The squared value of ``number``.
    """
    return number * number
